apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-claim
  namespace: minio
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: portworx-shared-gp1
